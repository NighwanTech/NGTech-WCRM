import MarketingLayout from '@/app/(marketing)/layout';

export default function WhatsAppCrmCityLayout({ children }: { children: React.ReactNode }) {
  return <MarketingLayout>{children}</MarketingLayout>;
}
